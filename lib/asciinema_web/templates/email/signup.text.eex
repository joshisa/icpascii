Welcome,

Click the following link to create your account for your Asciinema kubernetes deployment at <%= Enum.join([AsciinemaWeb.Endpoint.url,"/asciinema"],"") %>:

<%= @signup_url %>


If you did not initiate this request, just ignore this email. The request will expire shortly.
