Hello,

Click the following link to log in to your Asciinema kubernetes deployment at <%= Enum.join([AsciinemaWeb.Endpoint.url,"/asciinema"],"") %>:

<%= @login_url %>


If you did not initiate this request, just ignore this email. The request will expire shortly.
