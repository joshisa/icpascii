h2 Browse

= category_links current_category do |categories|
  = categories.link_to 'All public', "/asciinema".concat(browse_path), :public
  = categories.link_to 'Featured', "/asciinema".concat(category_path(:featured)), :featured
