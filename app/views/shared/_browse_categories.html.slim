h2 Browse

= category_links current_category do |categories|
  = categories.link_to 'All public', "/#{ENV['RAILS_RELATIVE_URL_ROOT']}".concat(browse_path), :public
  = categories.link_to 'Featured', "/#{ENV['RAILS_RELATIVE_URL_ROOT']}".concat(category_path(:featured)), :featured
